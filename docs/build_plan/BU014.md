# BU014 — Add Session Status to UI

Status:
Pending

Goal:
Display the transcription and summarization status for each session in the UI list.

Context:
Users need to know the state of each session at a glance (e.g., "Not transcribed", "Transcribed", "Summarized"). This BU adds that information to the session list created in BU013.

Allowed Files:
- `src/app/window.py`
- `src/storage/database.py`
- `src/app/session_manager.py`

Dependencies:
Required:
- BU013

Blocks:
- BU015

Tasks:
- If necessary, modify the database schema in `db/database_manager.py` to include status fields for transcription and summarization.
- Update the session retrieval method in `db/database_manager.py` to include the status fields.
- Modify `ui/main_window.py` to display the status alongside each session in the list.
- Update `session_manager.py` to set the session status when transcription or summarization is completed.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- The UI must display the transcription and summarization status for each session.
- The status must be accurate based on the data in the database.

Tests Required:
- no

Out of Scope:
- Triggering transcription or summarization.
- Any UI elements other than the status display.

Definition of Done:
[ ] The session list in the UI shows the transcription status for each session.
[ ] The session list in the UI shows the summarization status for each session.

Reviewer Criteria:

Scope Validation:
[ ] Only the status display is implemented.
[ ] `Allowed Files` respected.

Functional Validation:
[ ] The status is displayed correctly for all possible states (e.g., none, transcribed, summarized).

Architecture Validation:
[ ] The database schema changes are minimal and effective.

Code Quality:
[ ] Readable
[ ] Minimal

Documentation Validation:
[ ] `current_state.md` updated on completion.
[ ] `devlog.md` appended on completion.

Next:
BU015
