# BU017 — Trigger Summarization from UI

Status:
Pending

Goal:
Add a button to the UI to start the summarization process for a selected session.

Context:
Similar to transcription, users need a way to manually trigger summarization for sessions that have been transcribed but not yet summarized.

Allowed Files:
- `ui/main_window.py`
- `summary_generator.py`
- `session_manager.py`

Dependencies:
Required:
- BU015
- BU016

Blocks:
- BU018

Tasks:
- Add a "Summarize" button to the UI for each session that has been transcribed but not summarized.
- Implement the logic in `ui/main_window.py` to handle the button click.
- The button click should trigger the summarization process for the selected session.
- The UI should provide feedback that summarization is in progress.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- Clicking the "Summarize" button must start the summarization process for the correct session.
- The button should only be visible or enabled for sessions that are transcribed but not summarized.
- The UI should update to reflect the new status once summarization is complete.

Tests Required:
- no

Out of Scope:
- The summarization process itself.
- Triggering transcription.

Definition of Done:
[ ] A "Summarize" button is present for transcribed, un-summarized sessions.
[ ] Clicking the button starts summarization.
[ ] The UI updates to show the new status after summarization.

Reviewer Criteria:

Scope Validation:
[ ] Only the summarization trigger is implemented.
[ ] `Allowed Files` respected.

Functional Validation:
[ ] The summarization process is correctly triggered.
[ ] The UI provides appropriate user feedback.

Architecture Validation:
[ ] The UI correctly communicates with the backend.

Code Quality:
[ ] Readable
[ ] Minimal

Documentation Validation:
[ ] `current_state.md` updated on completion.
[ ] `devlog.md` appended on completion.

Next:
BU018
