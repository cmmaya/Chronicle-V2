# BU002 — SQLite Storage

Status:
Pending

Goal:
Implement SQLite database layer for sessions, transcripts, and metadata storage.

Context:
SQLite is source of truth for Chronicle. Must support session management, transcript storage, and screenshot metadata with timestamp synchronization.

Allowed Files:
- src/storage/database.py
- src/storage/__init__.py
- src/storage/models.py

Dependencies:
Required:
- BU001

Blocks:
- BU003
- BU004

Tasks:
- Create database schema for sessions table
- Create database schema for transcripts table  
- Create database schema for screenshots table
- Implement Database class with connection management
- Add session CRUD operations
- Add timestamp indexing for synchronization

Out of Scope:
- Audio file storage (filesystem only)
- Image file storage (filesystem only)
- Data migration
- Backup functionality

Definition of Done:
[ ] SQLite database creates successfully
[ ] Sessions table with id, name, start_time, end_time, status
[ ] Transcripts table with id, session_id, timestamp, text, source
[ ] Screenshots table with id, session_id, timestamp, filepath
[ ] Database class with connect/disconnect methods
[ ] Basic CRUD operations work

Reviewer Focus:
- Schema design
- Timestamp precision
- Connection handling

Next:
BU003