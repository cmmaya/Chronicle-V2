# BU071 — Create RAG Metadata Schema

Status:
Pending

Goal:
Add local SQLite tables required to represent searchable RAG documents and chunks.

Context:
Chronicle currently stores source data in domain tables. This BU creates normalized RAG metadata tables without changing existing behavior.

Allowed Files:
- storage/database.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- none

Blocks:
- BU072

Tasks:
- In Database._initialize_schema, create rag_documents with id, source_type, source_id, session_id, timestamp, title, content_hash, metadata_json, created_at, updated_at.
- In Database._initialize_schema, create rag_chunks with id, document_id, session_id, chunk_index, content, token_count, start_timestamp, end_timestamp, metadata_json, created_at.
- Add indexes for rag_documents(source_type, source_id), rag_documents(session_id), rag_chunks(document_id), and rag_chunks(session_id).
- Do not populate the tables in this BU.
- Update docs/current_state.md with BU071 status.
- Append concise BU071 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Create a temporary Database instance.
- Call connect().
- Verify rag_documents and rag_chunks exist in sqlite_master.
- Verify expected indexes exist.

Out of Scope:
- Do not create FTS tables.
- Do not implement search.
- Do not create embeddings.
- Do not modify assistant service behavior.

Definition of Done:
[ ] rag_documents table exists
[ ] rag_chunks table exists
[ ] required indexes exist
[ ] existing schema initialization still succeeds
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU072
