# BU073 — Add RAG FTS Rebuild

Status:
Pending

Goal:
Create a SQLite FTS5 table and a rebuild method that indexes existing rag_chunks.

Context:
Unified lexical retrieval needs FTS5 over normalized chunks. This BU creates the FTS table and rebuild operation only.

Allowed Files:
- storage/database.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU072

Blocks:
- BU074

Tasks:
- In _initialize_schema, create rag_fts using FTS5 with content, source_type, session_id UNINDEXED, document_id UNINDEXED, chunk_id UNINDEXED.
- Add rebuild_rag_fts() -> None.
- rebuild_rag_fts must delete existing FTS rows and insert all rag_chunks joined to rag_documents.
- Ensure rebuild works when there are zero chunks.
- Update docs/current_state.md with BU073 status.
- Append concise BU073 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Create one rag_document and one rag_chunk.
- Run rebuild_rag_fts().
- Query rag_fts directly and verify indexed content is present.
- Run rebuild_rag_fts() twice and verify no duplicate growth.

Out of Scope:
- Do not expose search methods.
- Do not build chunks from original tables.
- Do not modify assistant tools or service.

Definition of Done:
[ ] rag_fts table exists
[ ] rebuild_rag_fts indexes chunks
[ ] rebuild is idempotent
[ ] empty rebuild succeeds
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU074
