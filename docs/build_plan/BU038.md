# BU038 — Single Session Assistant Retrieval

Status:
Pending

Goal:
Retrieve summary, transcript excerpts, and screenshot references for one explicit session.

Context:
When the user is asking about the selected or active session, the answer service needs a single call that builds bounded context from SQLite-backed resources.

Allowed Files:
- src/assistant/context.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU037

Blocks:
- BU040
- BU041
- BU042

Tasks:
- Create AssistantContextRetriever with build_session_context(session_id, question).
- Use existing database methods for summaries and screenshots where available.
- Use or call an existing transcript listing method; if missing, fail with a clear TODO error for BU039 instead of adding database code here.
- Filter transcript excerpts with simple keyword matching and bounded limits.
- Return screenshot references near relevant transcript timestamps when possible.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Test context building with a fake db object.
- Test empty summaries/transcripts/screenshots returns an empty but valid context.
- Test transcript filtering limits prompt size.

Out of Scope:
- No database schema changes.
- No cross-session search.
- No OpenRouter calls.
- No UI code.

Definition of Done:
[ ] build_session_context returns deterministic context for an explicit session_id.
[ ] Context includes summary text when available.
[ ] Context includes bounded transcript excerpts.
[ ] Context includes local screenshot references only, not image uploads.

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU039
