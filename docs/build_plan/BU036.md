# BU036 — Assistant OpenRouter Client

Status:
Pending

Goal:
Add a minimal OpenRouter chat client for assistant responses.

Context:
Agent execution should be isolated behind a tiny client before orchestration and UI wiring. This keeps provider-specific behavior out of window.py and retrieval code.

Allowed Files:
- src/assistant/client.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- none

Blocks:
- BU038

Tasks:
- Create src/assistant/client.py with OpenRouterAssistantClient.
- Support model, system prompt, user prompt, and optional prior messages.
- Read API key from the same configuration pattern used by summarization.
- Raise clear runtime errors for missing API key or non-success API responses.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
no

If yes:
- none

Out of Scope:
- agent selection config
- database persistence
- UI changes
- streaming responses
- tool calling

Definition of Done:
[ ] client can build and send a valid OpenRouter chat completion request
[ ] missing API key fails with a clear error
[ ] non-2xx provider response fails with a clear error
[ ] no existing summarization code is modified

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU037
