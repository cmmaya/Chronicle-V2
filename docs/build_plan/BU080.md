# BU080 — Wire Any Session Service Retrieval

Status:
Completed

Goal:
Replace ad hoc any-session context assembly with unified search_everything plus the context builder.

Context:
AssistantAnswerService currently builds all-session context through separate session, summary, and transcript searches. This BU switches it to unified retrieval.

Allowed Files:
- assistant/service.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU079

Blocks:
- BU081
- BU082
- BU083

Tasks:
- Import build_any_session_context from assistant/rag_context_builder.py.
- In _get_all_sessions_context, first call self._tools.search_everything(question, limit=30).
- If unified search returns non-error results, return build_any_session_context(question, results).
- If unified search fails or returns no usable results, preserve the existing legacy all-session context fallback.
- Do not change ask() signature.
- Do not change conversation persistence.
- Update docs/current_state.md with BU080 status.
- Append concise BU080 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Fake tools returns search_everything results and service returns context builder output.
- Fake tools returns error and legacy fallback still runs.
- No change to ask() response shape.

Out of Scope:
- Do not remove legacy fallback.
- Do not modify resolver.
- Do not add embeddings.
- Do not change OpenRouter call behavior.

Definition of Done:
[x] _get_all_sessions_context uses search_everything first
[x] legacy fallback remains
[x] service ask signature unchanged
[x] conversation persistence unchanged
[x] current_state.md updated
[x] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU081
