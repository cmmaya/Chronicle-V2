# BU077 — Add Current Session FTS Retrieval

Status:
Pending

Goal:
Add a focused helper that retrieves relevant RAG chunks for one session.

Context:
Current-session retrieval must stop relying only on manual keyword scoring. This BU adds the helper without replacing existing context assembly.

Allowed Files:
- assistant/context.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU076

Blocks:
- BU078

Tasks:
- Extend DatabaseProtocol with search_rag_fts.
- Add _get_rag_chunks(session_id, question, limit=12) to AssistantContextRetriever.
- Call self._db.search_rag_fts(question, limit=limit, session_id=session_id) inside the helper.
- Convert database rows into plain dictionaries matching the fields of RetrievedChunk from assistant/rag_models.py, without modifying rag_models.py.
- Return an empty list on database errors.
- Do not yet change build_session_context output.
- Update docs/current_state.md with BU077 status.
- Append concise BU077 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Use a fake db with search_rag_fts.
- Verify helper passes session_id filter.
- Verify rows convert into RetrievedChunk.
- Verify database error returns empty list.

Out of Scope:
- Do not remove existing transcript keyword retrieval.
- Do not alter AssistantContext shape.
- Do not modify service.py.

Definition of Done:
[ ] _get_rag_chunks helper exists
[ ] helper is session-scoped
[ ] helper returns normalized chunk dictionaries
[ ] existing build_session_context behavior unchanged
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU078
