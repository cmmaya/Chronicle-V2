# BU039 — Assistant Database Search Methods

Status:
Pending

Goal:
Add safe database methods for transcript and summary search across sessions.

Context:
Questions about any session require global search, but the agent must not generate SQL directly. Database access remains behind controlled methods.

Allowed Files:
- src/storage/database.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU034

Blocks:
- BU040
- BU041

Tasks:
- Add get_transcripts(session_id) if not already present.
- Add search_transcripts(query, limit, session_id=None) using parameterized LIKE queries.
- Add search_summaries(query, limit, session_id=None) using parameterized LIKE queries.
- Add find_sessions(query, limit) using session names plus summary/transcript matches.
- Keep all query methods read-only.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Test get_transcripts returns chronological rows.
- Test search_transcripts is parameterized and supports optional session_id.
- Test search_summaries returns session metadata.
- Test find_sessions returns bounded candidate sessions.

Out of Scope:
- No assistant service logic.
- No UI changes.
- No free-form SQL from model input.
- No embeddings/vector database.

Definition of Done:
[ ] Database exposes safe read-only search methods.
[ ] All search methods use parameterized SQL.
[ ] Results include enough session metadata for resolver/context.
[ ] Existing database methods remain compatible.

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU040
