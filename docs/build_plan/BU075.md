# BU075 — Expose Unified Search Tool

Status:
Pending

Goal:
Expose the unified RAG FTS search through AssistantRetrievalTools with validation and bounded limits.

Context:
The answer service should not access the database directly. Tools provide the safe retrieval boundary.

Allowed Files:
- assistant/tools.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU074

Blocks:
- BU076
- BU079

Tasks:
- Extend DatabaseLike protocol with search_rag_fts.
- Add search_everything(query, limit=20, session_id=None, source_types=None) -> list[dict].
- Validate non-empty query.
- Validate and cap limit to 50.
- Validate session_id when provided.
- Allow only known source types: transcript, summary, screenshot, assistant_message.
- Return normalized dictionaries without raw SQL details.
- Update docs/current_state.md with BU075 status.
- Append concise BU075 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Use a fake db implementing search_rag_fts.
- Verify validation rejects empty query.
- Verify source type whitelist rejects unknown values.
- Verify limit cap is applied.
- Verify returned result shape is stable.

Out of Scope:
- Do not modify AssistantAnswerService.
- Do not modify context.py.
- Do not implement indexing from source tables.

Definition of Done:
[ ] search_everything tool exists
[ ] tool validates inputs
[ ] tool enforces source type whitelist
[ ] tool output is normalized
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU076
