# BU084 — Add Local Embedding Schema Placeholder

Status:
Pending

Goal:
Prepare the SQLite schema for local embeddings without implementing semantic search.

Context:
Embeddings should be introduced only after lexical RAG is stable. This BU adds a minimal storage placeholder.

Allowed Files:
- storage/database.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU083

Blocks:
- none

Tasks:
- Add nullable embedding_model TEXT column to rag_chunks if it does not already exist.
- Add nullable embedding BLOB column to rag_chunks if it does not already exist.
- Add index on rag_chunks(embedding_model).
- Do not compute embeddings.
- Do not add embedding dependencies.
- Update docs/current_state.md with BU084 status.
- Append concise BU084 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- yes

If yes:
- Schema initialization succeeds on a new database.
- Migration succeeds on an existing database with rag_chunks.
- rag_chunks has embedding_model and embedding columns after initialization.

Out of Scope:
- Do not implement vector search.
- Do not install sentence-transformers.
- Do not change retrieval ranking.
- Do not modify assistant service.

Definition of Done:
[ ] embedding_model column exists
[ ] embedding column exists
[ ] embedding_model index exists
[ ] no embedding computation added
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
none
