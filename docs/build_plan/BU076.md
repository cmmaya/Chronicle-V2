# BU076 — Create RAG Result Models

Status:
Pending

Goal:
Create typed data models for normalized retrieval results.

Context:
Structured retrieval models reduce dictionary drift before current-session and any-session retrievers are updated.

Allowed Files:
- assistant/rag_models.py
- docs/current_state.md
- docs/devlog.md

Dependencies:
Required:
- BU075

Blocks:
- BU077

Tasks:
- Create assistant/rag_models.py.
- Add SourceType enum with transcript, summary, screenshot, assistant_message.
- Add RagScope enum with current_session and any_session.
- Add RetrievedChunk dataclass with chunk_id, document_id, source_type, source_id, session_id, timestamp, title, content, score.
- Add RetrievalBundle dataclass with query, scope, chunks.
- Keep models dependency-free except standard library.
- Update docs/current_state.md with BU076 status.
- Append concise BU076 entry to docs/devlog.md.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- executable behavior verified
- no syntax/runtime errors
- Definition of Done satisfied

Tests Required:
- no

If yes:
- none

Out of Scope:
- Do not use these models in service yet.
- Do not change database code.
- Do not add serialization helpers unless necessary.

Definition of Done:
[ ] rag_models.py created
[ ] SourceType exists
[ ] RagScope exists
[ ] RetrievedChunk exists
[ ] RetrievalBundle exists
[ ] module imports without runtime errors
[ ] current_state.md updated
[ ] devlog.md appended

Reviewer Criteria:

Scope Validation:
[ ] only requested scope implemented
[ ] no future BU leakage
[ ] Allowed Files respected

Functional Validation:
[ ] feature works as expected
[ ] validation requirements satisfied
[ ] failure modes acceptable

Architecture Validation:
[ ] repository structure preserved
[ ] no unnecessary abstractions
[ ] implementation consistent with project_brief

Code Quality:
[ ] readable
[ ] minimal
[ ] no obvious dead code

Documentation Validation:
[ ] current_state updated
[ ] devlog appended

Next:
BU077
