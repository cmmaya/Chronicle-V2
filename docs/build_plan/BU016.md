# BU016 — Configure Summarization Agent

Status:
Pending

Goal:
Update the `SummaryGenerator` to use the `minimax-M2` model from OpenRouter and allow for custom instructions.

Context:
The user has requested a specific model and the ability to provide custom instructions for summarization. This BU updates the backend to meet these requirements.

Allowed Files:
- `summary_generator.py`
- `config.py` (or a similar configuration file)

Dependencies:
Required:
- None

Blocks:
- BU017

Tasks:
- Modify `summary_generator.py` to use the `minimax-M2` model when calling the OpenRouter API.
- Add a mechanism to load custom summarization instructions from a configuration file (e.g., `config.py`).
- The `SummaryGenerator` should use these custom instructions in the prompt sent to the API.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- The `SummaryGenerator` must call the OpenRouter API with the `minimax-M2` model.
- The `SummaryGenerator` must include the custom instructions from the configuration file in the API request.

Tests Required:
- no

Out of Scope:
- Any UI changes.
- Triggering the summarization process.

Definition of Done:
[ ] The `SummaryGenerator` is configured to use the `minimax-M2` model.
[ ] The `SummaryGenerator` loads and uses custom instructions.

Reviewer Criteria:

Scope Validation:
[ ] Only the `SummaryGenerator` configuration is changed.
[ ] `Allowed Files` respected.

Functional Validation:
[ ] The summarization process still works correctly with the new model and instructions.

Architecture Validation:
[ ] The use of a configuration file for custom instructions is implemented cleanly.

Code Quality:
[ ] Readable
[ ] Minimal

Documentation Validation:
[ ] `current_state.md` updated on completion.
[ ] `devlog.md` appended on completion.

Next:
BU017
