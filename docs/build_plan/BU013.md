# BU013 — Display Past Sessions in UI

Status:
Pending

Goal:
Display a list of past sessions from the database in the main UI.

Context:
The application needs a way to show users their previous sessions so they can interact with them (e.g., transcribe, summarize, view details). This is the first step towards building a persistent session management UI.

Allowed Files:
- `ui/main_window.py`
- `db/database_manager.py`

Dependencies:
Required:
- None

Blocks:
- BU014

Tasks:
- Modify `db/database_manager.py` to add a method that retrieves all past sessions.
- Modify `ui/main_window.py` to call the new database method on startup.
- Add a list widget to the main window to display the sessions.

Execution Sequence:
1. Understand
2. Implement
3. Validate
4. Review Readiness
5. Document

Validation Requirements:

Required:
- The main window must display a list of all sessions stored in the database upon launch.
- Each item in the list should be clearly identifiable as a session.

Tests Required:
- no

Out of Scope:
- Displaying session status.
- Any interaction with the list items (e.g., clicking, selecting).
- Creating new sessions from the UI.

Definition of Done:
[ ] The main window displays a list of past sessions.
[ ] The session list is populated from the database.

Reviewer Criteria:

Scope Validation:
[ ] Only the session list display is implemented.
[ ] No interaction or status display is included.
[ ] `Allowed Files` respected.

Functional Validation:
[ ] The UI correctly fetches and displays the session list.
[ ] The application runs without errors.

Architecture Validation:
[ ] The UI and database logic are kept separate.

Code Quality:
[ ] Readable
[ ] Minimal

Documentation Validation:
[ ] `current_state.md` updated on completion.
[ ] `devlog.md` appended on completion.

Next:
BU014
